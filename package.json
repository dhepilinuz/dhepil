{
  "name": "dhepil",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "scripts": {
    "dev": "pnpm --filter @apps/web dev",
    "build": "pnpm --filter @apps/web build",
    "typecheck": "pnpm --filter @apps/web typecheck || exit 0",
    "lint": "pnpm --filter @apps/web lint || exit 0",
    "clean": "git clean -xfd -e .vscode -e .env*"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
